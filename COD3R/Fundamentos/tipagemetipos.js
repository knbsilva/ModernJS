// TIPAGEM

// TIPOS

const peso1 = 1.0;
const peso2 = Number('2.0');

console.log(typeof peso2);
console.log(Number.isInteger(peso1));
console.log(Number.isInteger(peso2));

// (variavel.toFixed(x)) função que defini a quantidade de casa decimais
// (variavel.toString(x)) função para transformar uma variável em string

