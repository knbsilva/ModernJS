// for in
let notas = []

for (let i in notas){
console.log(i, notas[i])
}

const pessoa = {
    nome : 'kleber',
    idade: 'silva',
    peso: '60kg'
}

for (atributos in pessoa){
    console.log(`${atributos} = ${pessoa[atributos]}`)
}

// break - continue
// breah -> fora do laço
// continue -> continua no laço

externo: for (a in num){ // externo igual a rótulo
}

